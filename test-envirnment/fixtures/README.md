# Put demo fixtures here.
#
# Already included:
#   sample.md
#   sample.txt
#
# YOU must upload (required for pass/fail confidence):
#   sample.pdf
#   sample.png   (or sample.jpg / sample.webp — preferably with readable text)
#
# Then set OPENAI_API_KEY + DATABASE_URL in ../.env and run: npm start
